spring:
  datasource:
    url: jdbc:mysql://202.194.14.120:3306/java_2_52?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=true&jdbcCompliantTruncation=false
    username: java_2_52
    password: JavaP252@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 1010

# JWT Configuration
jwt:
  secret: sms-server-secret-key-2024-spring-boot-javaSMS-server-v1.0
  expiration: 86400000  # 24 hours in milliseconds
  refresh-expiration: 604800000  # 7 days in milliseconds

# AI Grading Configuration
ai:
  api:
    url: ${AI_API_URL:https://api.deepseek.com/chat/completions} # AI模型的URL地址
    key: ${AI_API_KEY:sk-bd216c53cb5f48878bcaa063bce40765} # AI模型的API密钥。注：该密钥为测试使用密钥，会在几个月后失效，失效后请替换为新的密钥。
  model: ${AI_MODEL:deepseek-v4-flash} # AI模型的名称

# MyBatis Configuration
mybatis:
  configuration:
    map-underscore-to-camel-case: true

